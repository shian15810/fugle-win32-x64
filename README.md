# fugle-win32-x64

```bash
$ nativefier "https://www.fugle.tw/?p=win32&v=${FUGLE_APP_DESKTOP_VERSION}" --name "Fugle" --inject "./fugle-app-prerequisite/not-for-app.css" --icon "./fugle-app-prerequisite/fugle.png" --platform "win32" --arch "x64"
```
